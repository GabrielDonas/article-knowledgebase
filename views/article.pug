extends layout

block content
    h1= article.title
    h5 Written by #{article.author}
    p= article.body
    a.btn.btn-dark(href='/article/edit/' + article._id) Edit
    a.btn.btn-danger.ml-2.delete-article(href='#', data-id=article._id) Delete